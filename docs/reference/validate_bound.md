# Function to validate if a created boundary satisfies a given instance row

Function to validate if a created boundary satisfies a given instance
row

## Usage

``` r
validate_bound(b, cols, instance_row)
```
