describe("generate_cutpoints", {
  # basic functionalities
  it("should return a list of upper and lower bounds of each column of interest", {

  })
  it("should get midpoints of each cut point", {

  })
  # error behaviors
  it("should throw an error when there is only data point")
})

describe("create_anchro_inst", {
  it("should return a class of anchor")
  # edge cases
  it("should handle ")
})